import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme';
import * as ActivityControls from '../src/components/TimeManagementApp';

describe('TimeManagementApp general test', function () {
    it('its a fake test to load sources', function(){
        expect(true).to.equals(true);
    })
})